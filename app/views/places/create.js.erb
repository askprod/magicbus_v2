$('#login-form').slideUp();

$(document).ready(function(){
var elmnt = document.getElementById("map");
elmnt.scrollIntoView({ behavior: 'smooth', block: 'center' })
});

executeRecaptchaForShare();

var captchaReponse = $('#g-recaptcha-response-data-share').val();
$('#g-recaptcha-response').val(captchaReponse);

window.location.reload();
