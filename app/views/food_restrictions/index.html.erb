<p id="notice"><%= notice %></p>

<h1>Food Restrictions</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @food_restrictions.each do |food_restriction| %>
      <tr>
        <td><%= link_to 'Show', food_restriction %></td>
        <td><%= link_to 'Edit', edit_food_restriction_path(food_restriction) %></td>
        <td><%= link_to 'Destroy', food_restriction, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Food Restriction', new_food_restriction_path %>
