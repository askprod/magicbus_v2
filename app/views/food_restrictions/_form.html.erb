<%= form_with(model: food_restriction, local: true) do |form| %>
  <% if food_restriction.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(food_restriction.errors.count, "error") %> prohibited this food_restriction from being saved:</h2>

      <ul>
      <% food_restriction.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
