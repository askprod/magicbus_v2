<%= form_with(model: food_diet, local: true) do |form| %>
  <% if food_diet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(food_diet.errors.count, "error") %> prohibited this food_diet from being saved:</h2>

      <ul>
      <% food_diet.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
