$('#alert').html("<%= escape_javascript(render :partial => 'layouts/notice') %>").show();
$("#trips-sort").html("<%= escape_javascript(render partial: 'trips', locals: { trips: @sort_by_dates } ) %>"); 

$(document).ready(function(){
    var elmnt = document.getElementById("trips-sort");
    elmnt.scrollIntoView({ behavior: 'smooth' })
});